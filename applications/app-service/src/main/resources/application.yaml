##Spring Configuration
server:
  port: 8080
spring:
  application:
    name: msvc-authentication
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: /h2
  profiles:
    include:

adapters:
  r2dbc:
    host: "localhost"
    port: 5432
    database: "crediya_db"
    schema: "public"
    username: "postgres"
    password: "sasa"